# Hugo build output — generated, never committed
hugo/public/
hugo/resources/

# Hugo module cache
hugo/.hugo_build.lock

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Lambda invoke output — local testing artifact
response.json
function.zip

scratch.txt
*-security-findings.md
*-Security-Review.md
*-AUDIT.md

# Tracker SPA config — real values generated in CI from GitHub Actions vars; use config.example.js locally
app/train/src/config.js
config/canary.zip
config/canary-zip/
.claude/
linkedin.md