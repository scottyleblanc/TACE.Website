// Real values are not committed. In CI they are generated from GitHub Actions variables.
// For local development, copy this file to config.js and fill in real values.
export const CLIENT_ID   = '<COGNITO_APP_CLIENT_ID>';
export const AUTH_DOMAIN = 'https://<COGNITO_DOMAIN>.auth.ca-central-1.amazoncognito.com';
export const API         = 'https://<API_GATEWAY_ID>.execute-api.ca-central-1.amazonaws.com';
